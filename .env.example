# Vite HMR Configuration
# Copy this file to .env.local and adjust values for your network setup
VITE_HMR_HOST=localhost
VITE_HMR_PORT=5173

# Note: For network access from other devices (mobile testing):
# 1. Find your computer's IP address (ipconfig on Windows, ifconfig on Mac/Linux)
# 2. Set VITE_HMR_HOST to your IP address (e.g., 192.168.1.100)
# 3. Make sure firewall allows port 5173
